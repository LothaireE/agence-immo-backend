const assert = require("assert");
describe("Groupe de tests", () => {
  it("simple test", (done) => {
    assert.ok(true);
    done();
  });
  it("deuxieme test", (done) => {
    assert.ok(true);
    done();
  });
});
